export const password = (value) => {
  // if (value.length < 6) return 'Must contain atleast 6 charactures';
  // if (!/[a-z]/g.test(value)) return 'Must contain atleast one lowercase letter';
  // if (!/[A-Z]/g.test(value)) return 'Must contain atleast one uppercase letter';
  // if (!/[0-9]/g.test(value)) return 'Must contain atleast one number';

  return null;
}